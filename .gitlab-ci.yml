image: node

stages:
  - cache
  - build
  - test
  - deploy
  - deployment-test

cache:
  key: ${CI_COMMIT_REF_SLUG}
  policy: pull
  paths:
    - node_modules/

update cache:
  stage: cache
  only:
    - schedules
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    policy: push
    paths:
      - node_modules/
  script:
    - npm install

build website:
  stage: build
  except:
    - schedules
  script:
    - echo $CI_COMMIT_SHORT_SHA
    - npm install
    - npm install -g gatsby-cli
    - gatsby build
    - sed -i "s/%%VERSION%%/$CI_COMMIT_SHORT_SHA/" ./public/index.html
  artifacts:
    paths:
      - ./public

test artifact:
  image: alpine
  stage: test
  except:
    - schedules
  cache: {}
  script:
    - grep -q "Gatsby" ./public/index.html

test website:
  stage: test
  except:
    - schedules
  script:
    - npm install
    - npm install -g gatsby-cli
    - gatsby serve &
    - sleep 3
    - curl "http://localhost:9000" | tac | tac | grep -q "Gatsby"

deploy to surge:
  stage: deploy
  except:
    - schedules
  script:
    - npm install -g surge
    - surge --project ./public --domain bubblespot.surge.sh
      
test deployment:
  image: alpine
  stage: deployment-test
  except:
    - schedules
  cache: {}
  script:
    - apk add --no-cache curl
    - curl -s "http://bubblespot.surge.sh" | grep -q "Hi people"
    - curl -s "http://bubblespot.surge.sh" | grep -q "$CI_COMMIT_SHORT_SHA"
